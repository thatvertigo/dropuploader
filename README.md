# DropUploader

A dead simple drag-and-drop file uploader supporting a subset of ShareX configs written in Swift. Supports uploading via an in-app pane, opening a file through finder, or dragging a file onto its icon in the dock.
